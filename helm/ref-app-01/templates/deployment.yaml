apiVersion: apps/v1
kind: Deployment
metadata:
  name: ref-app-01
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: ref-app-01
  template:
    metadata:
      labels:
        app: ref-app-01
        spec:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      containers:
      - name: nginx
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        volumeMounts:
        - name: html
          mountPath: /usr/share/nginx/html
      volumes:
      - name: html
        configMap:
          name: ref-app-01-index-html