package repository

import (
	"testing"
)

// Repository integration tests require PostgreSQL; run manually against a real DB if needed.
func TestUserRepository_Placeholder(t *testing.T) {
	t.Log("use integration tests or pgxmock for full repository coverage")
}
